// Point this at your backend. For local development, the FastAPI default
// (uvicorn app.main:app --reload) serves on port 8000.
// After deploying the backend (e.g. to Render), replace this with that
// Points at the deployed backend on Render.
const API_BASE_URL = "https://fridge-manual-rag-api.onrender.com";
